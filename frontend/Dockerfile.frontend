# 前端服务 Dockerfile
FROM node:16-alpine

# 设置工作目录
WORKDIR /app

# 复制依赖文件
COPY package.json package-lock.json ./

# 安装依赖
RUN npm install

# 复制源代码
COPY . .

# 构建前端应用
RUN npm run build

# 安装 serve 用于提供静态文件
RUN npm install -g serve

# 暴露端口
EXPOSE 5173

# 启动前端服务
CMD ["serve", "-s", "dist", "-l", "5173"]
